# 2026-08-10

## 盘中盯盘自动化首次执行（13:22）
- 自动化 automation-1786335915429「盘中盯盘提醒」跑通，本地后端 8723 正常响应。
- 扫描自选股 4 只，全部「持有」，无买入/卖出信号，无预警命中。
- 发现问题：`/api/alerts` 为空，用户尚未配置任何涨破/跌破价格预警，导致预警检查环节形同虚设。建议补配。
- 接口理解要点（避免误判）：
  - `/api/screener` 的 `count` 是扫描数量而非命中数量。
  - composite 策略只输出「买入及以上」，卖出/减仓必须逐只 `/api/advice`。
  - `/api/advice` 用日线（最后一根为昨收），盘中价需 `/api/quotes` 对照。

## 盘中盯盘第二次执行 + 后端故障排查（14:25）
- 现象：8723 后端半瘫——config/watchlist/alerts 返回 200，但 advice/quotes/kline/signal/search 全部空回复（HTTP 000、curl exit 52，服务端异常断连）。
- 定位：同一份磁盘代码在全新 Python 进程中跑完全正常；进程 26956 启动于 12:23:45 晚于所有源文件修改，排除旧代码 → 判定为长驻进程运行期状态劣化。
- 处置：不重启用户进程，临时以 `PORT=8724 python server.py` 起干净实例完成扫描，事后 Stop-Process 清理，只留原进程。
- 扫描结果：4 只自选股全「持有」（茅台 -15 / 五粮液 -3 / 平安 -15 / 宁德 +14），无买卖信号，alerts 依旧为空。

### 代码缺陷（待修，值得动手）
- `server.py:346-347` `_run_screener` 对每只标的 `except: continue`，**数据源全挂时 results 也是空数组**，与「真无信号」无法区分 → 会把后端故障静默误报成「无信号，继续观望」。建议：统计失败数，随响应返回 `errors`/`fetched` 字段，前端与自动化据此区分。
- `/api/quotes`(197)、`/api/advice`(220)、`/api/kline`(189)、`/api/search`(178) 均无 try/except，数据源抛异常即断连、无任何错误体。建议统一包一层异常处理返回 `{"ok":false,"msg":...}`。
- `Handler.log_message` 被静默(129-130)，故障时无任何日志可查，排障全靠外部探测。建议至少把异常打到 stderr 或落盘。

## 盘中盯盘第三次执行（15:25）
- 8723 仍是同一个劣化进程 **PID 26956**（用户未重启），故障表现与 14:25 逐字一致 → 确认该劣化**不会自愈**。
- 再次走 8724 临时实例方案完成扫描，事后用 PowerShell 工具停掉临时进程 8768，用户进程 26956 未动。
- 结果：4 只自选股 action 全「持有」，评分与 14:25 完全相同（advice 基于日线昨收，同一交易日内多轮扫描不变属正常）；无买卖信号，alerts 仍为空。
- 收盘快照：茅台 1348.86 +3.03%、五粮液 76.19 +1.44%、平安 53.32 -0.11%、宁德 393.87 +1.86%。四只里三只飘红，但日线技术面评分仍未转多。
- 操作约束记录：**禁止从 Bash 调用 powershell**（安全策略拦截），需要杀进程/查端口时直接用 PowerShell 工具。
- 待办优先级上调：上述三条 server.py 缺陷已连续两轮造成实际误判风险，建议下次会话直接动手修，并为 server.py 增加健康检查/自愈。

## 16:22 盘中盯盘扫描（自动化 automation-1786335915429）
- 8723 后端已由用户重启，此前 14:25/15:25 的进程劣化问题解除，数据源全部正常。
- 收盘数据计入后自选股信号首次分化：茅台 sh600519 +29「买入」、平安 sh601318 -26「减仓」、五粮液 +12 / 宁德 +4「持有」。
- 待办：alerts 预警清单仍为空（连续 4 轮），需用户补配关键价位。
- 待排查 bug：/api/advice 对 sh600519 的 reasons 同时输出「MACD 金叉」与「MACD 死叉」，互斥信号疑似重复计入，需检查 core 下的信号生成与打分逻辑。

## 18:16 盘中盯盘扫描（自动化 automation-1786335915429）
- 后端 8723 正常。**王总已整体更换自选股**：现为 sh600105 永鼎股份、sz002475 立讯精密、sh588160 科创新材料ETF南方、sh588940 科创50ETF富国（旧的茅台/五粮液/平安/宁德已移除）。
- 信号：买入 sh588160（+30，建议 13100 股 ≈¥15,000 / 15% 仓位）；减仓 sz002475（-27，空头排列）；持有 sh600105（+18）、sh588940（-5，RSI 超卖）。
- alerts 仍为空（连续 5 轮）。16:22 记录的 MACD 金叉/死叉并存 bug 本轮未复现，仍待排查。
