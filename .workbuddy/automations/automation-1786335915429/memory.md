# 盘中盯盘提醒 — 执行记录

## 2026-08-10 13:22
- 后端 127.0.0.1:8723 在线，配置 scope=watchlist / strategy=composite / top_n=10。
- 自选股 4 只（sh600519 sz000858 sh601318 sz300750），screener 结果为空 → 无「买入及以上」。
- 逐只 /api/advice 复核：4 只 action 均为「持有」，无卖出/减仓。
- /api/alerts 为空列表 → 尚未配置任何价格预警，check 必然无命中。已提醒用户补配。
- 结论：无买入/卖出信号，继续持有观望。

## 2026-08-10 14:25
- 用户后端进程（PID 26956，8723 端口）出现**部分故障**：config/watchlist/alerts 正常 200，但所有依赖数据源的接口（advice/quotes/kline/signal/search）全部空回复（HTTP 000, curl exit 52 = 服务端异常断连）。
- 排查：同一份磁盘代码在全新 Python 进程中运行完全正常（茅台实时价、K线 120 根均可取）→ 非代码问题，是该长驻进程运行期状态劣化。进程启动于 12:23:45，晚于所有源文件修改，排除旧代码。
- 处置：**未重启用户进程**，改在 8724 端口另起临时干净实例完成本次扫描，扫描后已 Stop-Process 14872 清理，仅保留用户原进程 26956。
- 扫描结果：4 只自选股 action 全为「持有」（茅台 -15 / 五粮液 -3 / 平安 -15 / 宁德 +14），无买入、无卖出减仓；alerts 仍为空。
- 结论：无买入/卖出信号，继续持有观望。已提示用户重启 8723 后端。

## 2026-08-10 15:25
- 8723 进程 **PID 26956 仍是 14:25 那个劣化进程**，用户未重启。故障表现完全一致：config/watchlist/alerts 200 正常，advice/quotes 全部 HTTP 000 + exit 52。
- 8723 的 screener 返回 `count:4, results:[]`——正是备忘录里的假阴性陷阱，未采信。
- 沿用既定方案：PORT=8724 起临时干净实例 → 数据源验证正常（茅台实时 1348.86）→ 完成扫描 → Stop-Process 8768 清理。用户进程 26956 全程未动。
- 扫描结果：4 只 action 全为「持有」（茅台 -15 / 五粮液 -3 / 平安 -15 / 宁德 +14，与 14:25 完全一致，因 advice 走日线昨收）；无买入、无卖出减仓；alerts 仍为空。
- 实时价（15:25，已收盘快照）：茅台 1348.86 +3.03%、五粮液 76.19 +1.44%、平安 53.32 -0.11%、宁德 393.87 +1.86%。
- 结论：无买入/卖出信号，继续持有观望。已第二次提示用户重启 8723。

### 经验备忘
- /api/screener?strategy=composite 只返回「买入及以上」，卖出/减仓信号必须逐只调 /api/advice 才能拿到。
- screener 返回的 count = 扫描标的数（len(codes)），不是命中数；results 为空才代表无信号，别误读。
- /api/advice 基于日线，最后一根 K 线是昨收；盘中实时价需另调 /api/quotes 对照。
- **重要陷阱**：server.py `_run_screener` 对每只标的 try/except…continue（第346-347行），抓数全失败时 results 也是空数组，与「真无信号」**无法区分**。因此 screener 为空时必须用 /api/quotes 或 /api/advice 验证数据源确实可用，否则会把「后端坏了」误报成「无信号，观望」。本次即命中此坑。
- 判断后端健康：curl -w "%{http_code}"，HTTP 000 + exit 52 = 服务端崩溃断连，不是「没数据」。
- 8723 若再次出现同类劣化，最省事的解法是让用户重启 server.py；临时起 PORT=8724 实例可在不打扰用户会话的前提下拿到数据。
- **劣化不会自愈**：26956 从 14:25 坏到 15:25 仍未恢复，只要用户不重启，后续每轮都必须走 8724 备用方案。开场先 curl 一次 /api/quotes 探活，比等 screener 出空结果更快定位。
- 清理临时进程**不能从 Bash 调 powershell**（会被安全策略拦截：Invoking PowerShell from Bash）。必须直接用 PowerShell 工具，`Get-NetTCPConnection -LocalPort 8724 -State Listen` 取 OwningProcess 再 Stop-Process。
- 收盘后（>15:00）跑扫描，/api/quotes 返回的是收盘快照；advice 仍用昨收算分，故盘中多轮扫描 score 不变属正常，不要误判为数据没刷新。

## 2026-08-10 16:22
- **8723 后端已恢复正常**（用户重启了 server.py）。开场 /api/quotes 探活直接 200 且有数据，无需再走 8724 备用方案。劣化问题终结。
- 首次出现非「全员持有」结果：今日收盘数据计入日线后，茅台从 -15 翻到 **+29「买入」**，平安从 -15 掉到 **-26「减仓」**。
- 扫描结果：买入 1（茅台 sh600519 +29）、减仓 1（平安 sh601318 -26）、持有 2（五粮液 +12、宁德 +4）。
- 茅台仓位建议受限：目标 15% = ¥15,000，但一手需 ¥134,886，¥100,000 本金买不起 1 手，target_shares=0。高价股在 10 万本金下会长期出现此情形。
- alerts 仍为空（连续第 4 轮），已再次提示用户补配价位。
- **发现潜在 bug**：茅台 /api/advice 的 reasons 同时包含「MACD 金叉」与「MACD 死叉」，两者互斥，疑似打分逻辑重复计入，已向用户提示排查。

### 经验补充
- screener 的 results 只含「买入及以上」，本轮茅台命中；但平安的「减仓」仍只能靠逐只 advice 拿到，验证了既有备忘录的判断。
- 日线数据在收盘后（约 15:00 之后）会刷新为当日收盘，故收盘前后同一标的 score 可能剧烈翻转（茅台 -15 → +29），这是正常的，不是数据错乱。

## 2026-08-10 18:16
- 后端 8723 持续正常（开场 /api/quotes 探活 200 有数据，第 2 轮健康）。配置未变：watchlist / composite / top_n=10。
- **自选股已被用户整体更换**：旧 4 只（茅台/五粮液/平安/宁德）全部移除，新为 sh600105 永鼎股份、sz002475 立讯精密、sh588160 科创新材料ETF南方、sh588940 科创50ETF富国。历史 score 对比失去连续性，从本轮起重新建立基线。
- 扫描结果：买入 1（sh588160 +30）、减仓 1（sz002475 -27）、持有 2（sh600105 +18、sh588940 -5）。
- sh588160 仓位建议可正常落地：13100 股 ≈¥15,000（15% 仓位）——低价 ETF 不受高价股「买不起一手」限制，与茅台情形形成对照。
- sz002475 减仓建议 note = 「当前无持仓，无需减仓」（advice 传参 current_shares=0 所致，属预期行为）。
- alerts 仍为空（连续第 5 轮），已再次提示补配。
- 未复现 16:22 的 MACD 金叉/死叉并存 bug（本轮 4 只 reasons 均自洽），该 bug 可能仅在特定数据形态下触发。

### 经验补充 2
- 自选股可能被用户随时更换，每轮应以 /api/watchlist 实际返回为准，不要沿用上一轮的标的假设。
- ETF（588 开头）无 PE、单价低，是 10 万本金下唯一能足额建到目标仓位的品种类型；高价白马常年 target_shares=0。

## 2026-08-10 19:15
- 后端 8723 正常（第 3 轮健康，开场 /api/quotes 探活 200）。配置与自选股均未变（watchlist / composite / top_n=10；4 只：sh600105、sz002475、sh588160、sh588940）。
- 扫描结果与 18:16 **完全一致**：买入 1（sh588160 +30）、减仓 1（sz002475 -27）、持有 2（sh600105 +18、sh588940 -5）。已收盘，日线不再变动，score 复现属正常。
- 收盘价快照：永鼎 37.67 -3.41%、立讯 55.79 -2.11%、588160 1.145 +0.17%、588940 0.900 -0.11%。
- sh588160 仓位方案：13100 股 ≈¥15,000（0%→15%）。sz002475 因 current_shares=0，note 仍为「无持仓无需减仓」。
- alerts 仍为空（连续第 6 轮），已再次提示补配。
- 本轮无异常、无 bug 复现。

### 经验补充 3
- 收盘后（>15:00）各轮扫描结果会完全复现，直到次日开盘。若连续两轮 score 完全相同且时间在收盘后，属正常，不必怀疑数据未刷新。
