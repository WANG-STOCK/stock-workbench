# 云端长期运行（24/7 盯盘）

目标：让后端常驻、不依赖你本机开着，配合已有的**盘中盯盘自动化**（每 15 分钟推送买/卖提醒）实现无人值守。

## 手机不在同一 WiFi / 跨网络访问（临时、免费、免账号）

局域网地址（`http://192.168.x.x:8723`）只有手机连**同一个 WiFi** 才能打开。手机用别的 WiFi 或流量时，需要把本机端口暴露成公网网址。

最简单：双击本目录的 **`启动公网访问.bat`**。
- 它会先确保后端在跑，再用 **Cloudflare 隧道（cloudflared）** 把 `localhost:8723` 暴露成一个 `https://xxxx.trycloudflare.com` 的公网网址（**免注册、免信用卡、能穿透 NAT/光猫**）。
- 把网址复制到手机浏览器即可，**任何网络都能开**。
- 关闭那个命令行窗口 = 立即断开公网访问。
- ⚠️ 该网址无登录鉴权，请勿外泄，仅自己使用；不用时随手关窗口。

备选（若 cloudflared 安装失败）：用 **ngrok** —— 下载 https://ngrok.com/download 安装后，在目录里执行 `ngrok http 8723`，同样得到一个公网网址。

> 想要**长期、稳定、24 小时**都能用（不用本机开着），见下方"云端长期运行"方案。

## 架构
```
[云主机] 运行 server.py (零依赖 Python)  :8723   ← 行情/信号/选股/预警
    ▲
    │  curl http://<公网IP>:8723/api/...
[WorkBuddy 自动化] 每个交易日 9:30–15:00 每15分钟扫描并推送提醒到你对话
    ▲
    │  浏览器（任意设备）
[前端] 把「云端后端地址」填成 http://<公网IP>:8723 即可远程盯盘
```

## 方式一：Docker（推荐，最省事）
1. 把整个 `stock-workbench/` 目录传到云主机（腾讯云/阿里云轻量应用服务器均可，1C1G 足够）。
2. 安装 Docker 后：
   ```bash
   cd stock-workbench
   docker compose up -d
   ```
3. 开放安全组/防火墙的 **8723** 端口（仅建议对你的 IP 放行，或加 nginx + 密码）。
4. 本地前端：打开工作台 → 侧栏「评分权重」→ 展开「云端后端地址」→ 填入
   `http://<公网IP>:8723` 并保存。之后即可远程查看与接收提醒。

## 方式二：裸 Python
```bash
cd stock-workbench
pip install --no-deps .   # 实际无第三方依赖
PORT=8723 python server.py
# 或用 nohup / systemd / supervisor 保活
```

## 让自动化指向云端（关键）
已有的「盘中盯盘提醒」自动化读取 `data/config.json` 的 `cloud_url`：
- 若 `cloud_url` 为空 → 默认访问 `http://127.0.0.1:8723`（你本机开着时）。
- 若填了云端地址 → 自动化改为访问云端后端，实现**本机不开也能推提醒**。

设置 `cloud_url`：
- 工作台 UI：暂未提供输入框，直接编辑 `data/config.json` 加一行 `"cloud_url": "http://<公网IP>:8723"`；
- 或调用 `POST /api/config` `{"cloud_url": "http://<公网IP>:8723"}`。

## 安全提醒
- 后端无任何鉴权，**不要把 8723 裸奔到公网**。至少：防火墙限 IP、前置 nginx 加 Basic Auth / 反向代理到内网。
- 行情来自新浪/腾讯公开接口，云主机需能访问外网。
- 本工具只做技术面信号与规则化建议，**不在云端自动下单**；真实委托仍需你在券商端确认。

## 静态前端部署（仅查看，不含后端）
`cloudstudio-deploy` 等静态托管只能放前端，没有 Python 后端。若仅想手机看界面，
可把 `static/` 托管到任意静态空间，并在页面「云端后端地址」填入上面的云后端 URL。
