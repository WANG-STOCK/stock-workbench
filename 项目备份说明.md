# 股票工作台 · 项目备份说明（2026-08-11）

> 本文件是给王总看的"双保险"说明，含固定链接、本地运行、推送部署、本次改动与注意事项。
> 投资建议仅供参考，不构成投资建议，实际操作以券商端为准。

---

## 一、这是什么
技术面股票工作台：左侧看 K 线 + 技术面信号（买 / 卖 / 持有建议），右侧管理持仓、自选盯盘、扫描候选。网页端和手机端通用，盘中自动刷新。

## 二、固定访问链接（不会变）
- **云端（手机每天打开这个就行）**：`https://wang-zhibiao-2026.onrender.com`
- 免费版 Render：每天**第一次**打开约等 1 分钟冷启动，刷新一下就好；之后秒开。
- 链接与服务名绑定，只要不删服务 / 不改服务名，永久不变。

## 三、本地运行（可选，在自己电脑看）
```
cd stock-workbench
python server.py          # 默认端口 8723
# 浏览器打开 http://127.0.0.1:8723
```

## 四、推送到 GitHub / 部署（本机网络受限，照做即可）
本机直连 `github.com:443` 会被挡，GitHub Desktop 报 `Failed to connect to github.com port 443` 时：

1. 确认 Clash / V2Ray 代理已开，记下本地 **HTTP 代理端口**（例如 2334）。
2. 按 `Win + R` → 输入 `cmd` → 回车，执行：
   ```
   git config --global http.proxy http://127.0.0.1:2334
   git config --global https.proxy http://127.0.0.1:2334
   ```
   （把 `2334` 换成你代理实际的端口；不需要时可 `git config --global --unset http.proxy` 撤销）
3. 回到 GitHub Desktop 点 **Push origin** 即可。
4. 推送后 Render **自动重新部署**，通常 1–3 分钟生效，手机刷新就能看到新版本。

## 五、本次（2026-08-11）改动
- **右侧布局改为双列**：左列「持仓 · 做T」，右列「自选盯盘 · 扫描候选」合并成一个单元，扫描结果直接显示在合并单元下方，无需整页下滚。
- 改动文件：`static/index.html`、`static/css/styles.css`（所有按钮 / 输入框功能不变，仅挪位置）。
- 服务名 / 链接：`render.yaml` 中 `name: wang-zhibiao-2026`；`data/config.json` 中 `cloud_url: https://wang-zhibiao-2026.onrender.com`。

## 六、注意事项
- **千万不要删除**项目里的 `.workbuddy` 文件夹（存对话记忆与项目数据，不是临时缓存）。
- 黄色 "LF / CRLF" 提示可忽略，不影响推送。
- 投资信号仅供参考，不构成投资建议。

## 七、记录如何找回（双保险）
- **代码**：本地磁盘（`D:\workbuddy\2026-08-10-11-22-24\stock-workbench`）+ GitHub 远端，双份备份。
- **对话 / 记忆**：WorkBuddy 云端长期保存，新会话可检索历史；关键经验也已写入长期记忆文件。
